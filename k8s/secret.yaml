# Template — do NOT commit real values.
# Create with:
#   kubectl create secret generic forkit-site-secret \
#     --from-literal=JWT_SECRET=$(openssl rand -hex 32) \
#     -n forkit
apiVersion: v1
kind: Secret
metadata:
  name: forkit-site-secret
  namespace: forkit
  labels:
    app: forkit-site
type: Opaque
stringData:
  JWT_SECRET: ""
