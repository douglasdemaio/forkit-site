# Template — do NOT commit real values.
# Create with: kubectl create secret generic forkme-mcp-secret \
#   --from-literal=FORKIT_JWT_TOKEN=<your-jwt> -n forkit
apiVersion: v1
kind: Secret
metadata:
  name: forkme-mcp-secret
  namespace: forkit
  labels:
    app: forkme-mcp
type: Opaque
stringData:
  FORKIT_JWT_TOKEN: ""
